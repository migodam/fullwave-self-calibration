# XINAN GPU 实验环境与范围

用户在本轮明确授权通过 Tailscale 使用 XINAN 的 RTX4060。Tailscale 实际在线节点地址为 100.121.97.39；最初提供的 .37 没有对应节点。随后确认用户名并成功登录。密码不写入代码、报告、配置或交付包。

远端唯一项目目录：`D:\home\research\SSH_workspace\Gaussian_A2`。解释器：`D:\python\python.exe`，Python3.10.9、PyTorch2.6.0+cu124；NVIDIA GeForce RTX4060 Laptop GPU，8GB显存。复用已有CUDA/Python，没有安装驱动或新CUDA基础设施。

单频N16在CPU复128与CUDA复64上对照已有SciPy VIE，检查接收场、D的伴随及隐式梯度。批量多频后端进一步与逐频后端对照场和梯度。底层采用非Hermitian BiCGSTAB，每次检查真实状态残差；没有将固定次数传播迭代当全波解。复64停止阈值为2e-5，最终残差允许1.5倍数值裕量；这只是离散求解残差，不是模型误差证书。

GPU实现是已知FFT体积分求解和隐式微分的工程实现，本身不宣称原创。多频批处理将共享材料的独立状态方程一起求解，不引入跨频传播或伪造频间物理耦合。

规模探针N64/N128/N256、12发射×64接收，记录单频正演+伴随及首次初始化成本。大型成像比较N128反演、N192单元积分数据，Gaussian K=16/64/144（每节点6参数）对照16384参数voxel。主目标是分开表示容量和计算成本；不自动继承SOM或概率分支的理论贡献。

协议采用无额外测量噪声的开发比较，保留不同网格/积分失配。共有Adam更新次数、步长、离散平滑罚项；不同参数化的优化条件/有效先验并不相同，所以此比较不是所有调参预算都闭合的论文验收。Gauss初始化为固定格点，voxel由K64初始图产生；不同K并非相同初始材料图，结果必须注明。试运行与正式运行使用不同run-name。

远端作业只在明确run-name目录写数据，源码和配置哈希冻结；checkpoint含优化器和累计成本。逐方法结果、实际数组和图需同步回本地后才能计入最终报告。远端跑着、写出checkpoint、通过脚本都不能称为TAP通过。

正式main128经历系统重启：Windows返回LastBootUpTime为2026-09-11 14:57:15 UTC，原SSH会话退出255，原进程已消失。重启原因未知。保留case0–17与case18 K16/K64；case18 K144检查点头部为零字节，隔离后从同配置种子重新计算。新进程19028延续case18–29；重建case18数据与原数组逐项完全相同。不可将此写成连续无中断运行。方法计时排除混合阶段case18后另报配对比值；全场数据、初始化、失去的部分工作与运行中断开销有不同记录完整度。
