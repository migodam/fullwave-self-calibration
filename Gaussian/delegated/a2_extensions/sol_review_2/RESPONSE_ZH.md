# Root 对第二轮评审的处理

G1：已在XINAN重跑当前fcc1版backend的SciPy独立场参考、D伴随和隐式梯度有限差分；结果同步为runs/a2/gpu/backend_checks_remote.json。CUDA场相对差5.30e-7，梯度方向差1.51e-4。batch检查仍单独保留，不能替代独立检查。

G2：main128首次在同一XINAN/RTX4060/Python环境启动（PID31320），但随后SSH断连并发现原计算已停止。Windows报告的系统启动时间为2026-09-11 14:57:15 UTC，落在原运行期间；按跨系统重启的两个执行阶段记录，重启原因未确诊。完整case0–17和case18的K16/K64结果保留。case18 K144检查点头部为零字节，已隔离，不能假装恢复成功；该方法从相同固定种子重新执行，随后继续剩余对象。源码和配置不变；新进程与硬件由外部执行记录单列。runner没有禁止跨环境resume，交付不据此宣称任意跨设备成本可比。

G3：耗时含每步checkpoint序列化/I/O，报告称优化与逐步存档时间，不称纯GPU时间。中断的K144未完成工作、第二次数据/算子初始化和重新生成成本应另列为运行开销，不能被单方法最终秒数掩盖；也不能称作无中断端到端时间。

G4：显存只按全进程累计峰值报告，不能从case字段比较不同表示的显存。

M8：已修正首轮回复；历史保存误差标量，图像只保存终点。M9：明确parameter_weights=[1,1,1]是固定度量记录，不支持改成任意权重后自动生效。

R1/R2：正式大规模配置是120步（80是runner默认值），noiseless开发比较；初始化/映射/有效正则不同，仍不宣称一般表示优越。完整120次反演同步前不做最终排序。概率与流形新理论保持各自证据范围，不借GPU参数规模越过TAP硬门槛。
