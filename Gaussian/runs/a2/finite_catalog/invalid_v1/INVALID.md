# Invalid v1 catalog run

Do not use these results as evidence.  The split pair incorrectly shrank both
axes and the new-family comparison excluded its required nesting of the old
family.  It is retained only for auditability.
