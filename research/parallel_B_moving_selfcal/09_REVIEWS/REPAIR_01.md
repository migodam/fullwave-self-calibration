# 实现修复 01：初始点环带边界舍入

S 组所有12份观测已生成。处理场景2的 ordinary_joint 时，条件增益经极坐标还原再计算 log(abs(g))，可能比 log(1.35) 大一个浮点舍入单位；SciPy因此拒绝初始点。此前完成的拟合不删除。原始异常日志保留为 S_INITIAL_ROUNDING_FAILURE.log。

冻结的 experiment.py 和物理模型不修改。新增 safe_run.py 在传给 least_squares 前检查边界越界量，仅允许≤1e-12的浮点越界回到原来的边界；超过该量仍抛异常。没有改变材料初值、物理域、噪声、漂移或成功阈值。

safe_run.py SHA256 = 31116790395aa30140ed7f90821ebda81b47c314fa9851f1ac4add719163a45c。

I组原进程继续使用冻结实现；如遇相同异常则从原始数组显式resume。S组从未完成的方法续跑。完成的旧结果和新结果分别保留各自源码哈希；此修复不计作算法改进。最终从干净目录复现以 safe_run.py 为入口。
