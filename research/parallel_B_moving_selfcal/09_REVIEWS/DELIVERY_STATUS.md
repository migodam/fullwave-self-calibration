# 交付与上传状态

完整计算在本轮运行环境内执行，不依赖GitHub Actions。17项单元检查通过；干净smoke重建S/I各第零场景，所有数组及两种方法的材料误差相同。没有宣称完整36场景第二次重放，更没有宣称实测硬件或连续误差证明。

GitHub create_tree 对主实验脚本experiment.py返回“无法确定请求的安全状态”拦截。停止该文件的上传，没有更换编码、拆分该源码或通过其他端点绕过。此前成功的model.py、vsw.py、dda.py、safe_run.py保留；缺少主实验和相关脚本时，PR不是完整可运行镜像。其余文档及紧凑审计结果正常提交，不将其冒充完整原始数据。

完整研究目录、原始数组、全部拟合初值、独立诊断、源码SHA和报告，通过本轮对话的Moving_Array_B1_Research.zip交付。GitHub提交范围在PR正文准确列明。数据包内不包含字体、API密钥或用户教材全文。

## 源码记录与收敛审计

对324份主拟合记录中所保存的每个源文件SHA256逐项检查，全部与交付文件一致。原IMPLEMENTATION_FREEZE.md手工填写的vsw.py SHA256漏抄了一个字符；冻结原文不修改。正确值为 `8e960c2052fa126427ec203eeefabc955e6ea554369dda7af3cf5fbbf6dfe509`，对应Git blob `546046bdc41ff61603f7104a5468b474b567bdab`。原始运行JSON使用正确实际源码哈希，不存在事后更换物理求解器。

所有相干主方法的各初值均报告优化终止成功，这不构成全局或物理正确证明。无相位方法所选最低目标分支报告收敛的场景数为S9/12、I7/12、X6/12；其材料阈值通过计数仍为12/12、7/12、5/12。两类标签不能混用，未收敛记录全部保留。

汇总文件只包含开发计数，不替代原始观测。没有将不同场景数、方法数、多初值数或在线前缀数加总成独立样本量。
