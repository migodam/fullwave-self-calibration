# 中断续接审计：执行前范围固定

本审计读取而不替换 B1 原始记录。审计起点为 685532825a15930b06656bee52649c8ab5a18edf。原324次主拟合、17项测试和smoke结果属于原执行记录；在找不到原始数组和主脚本时，不将其描述为本次独立重放。

当前运行时只找到旧Q5附件，没有找到原报告提到的 Moving_Array_B1_Research.zip。GitHub 07_EXPERIMENTS 下实际包含 AUDITED_SUMMARY.json 等紧凑文件，不含 README 中列举的 SUMMARY.json、OUTCOMES.csv 或 S/I/X 原始数组目录。因此当前不能从原始数组独立核对各组成功计数。摘要可以引用，但不是原始证据的替代物。此前 experiment.py 上传被平台拦截，本次不重编码、改名或重写等价主脚本来规避。

可读取的 model.py、vsw.py、dda.py 将按Git blob和SHA256逐字核对后执行以下有限检查：匹配本构先验的Born尺度恒等式；理想直接交叉极化场为零；非零共极化直接路径对同一尺度变换的破缺；2500ps延迟别名；给定完整2×2耦合响应的逆矩阵校准恒等式与无耦合歧义；公共复增益完成平方；插值与直接读取一致性；同一固定材料对的全波尺度残差；一个固定材料点的DDA/VSW交叉场差；阶数4与6的交叉场差。所有这些是诊断/恒等式核对，不是全材料域覆盖或新的恢复成功率。

固定几何：原B1几何、八帧、三个频率；材料s=(2,2.7)，伙伴1.12s；有效偏移为linspace(-1,1,8)mm；另对零偏移报告固定点场差。DDA原h=.0065，不因看到差异而改成另一主结果。验证随机种子2026091199，仅用于有限代数检查。失败和实际执行结果原样保留。

交付将称为“续接审计包”，不是缺失的完整原始研究包；原报告关于此前全量附件交付的文字作为历史记录保留，但不用于本次可用性承诺。
