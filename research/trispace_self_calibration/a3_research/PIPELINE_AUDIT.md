# Actual installed AI Scientist execution and parent review

The installed run_development_loop was launched from run_pipeline.py on 2026-09-07. Invocation settings and absolute experiment directory are in pipeline_invocation.json. DeepSeek Pro evaluation and the named Flash coding adapter were used without changing the user's default provider/model.

Round zero generated the bounded four-theorem test package: 20 cases (16 positive, four deliberately invalid/negative controls). Parent freshly ran build_cases into results/pipeline_replication.json and explicitly checked the zero-denominator and small-residual controls rather than trusting their status labels. Parent's independent correlated-noise field-space gain quotient test additionally covers the raw field h weighting that the pipeline's log-coordinate fixture alone does not.

Round one drifted into generic linearized pose-graph/bundle-adjustment examples. Those are NOT electromagnetic full-wave validation and will not be used as such. The evaluator also interpreted the reused-validation negative control as a defect requiring the valid independent-validation theorem to be repaired. That interpretation is rejected: the control intentionally violates independence, and its failure is evidence for the assumption, not a contradiction of the conditional theorem. Its additional generated results are retained for provenance but not adopted as core paper evidence.

The automatic writeup failed looking for relative ai_scientist/blank_arr_latex while the process working directory was the project, and the bundled template is ACL rather than IEEE TAP. No automatic PDF/review acceptance occurred. Parent will produce the TAP manuscript separately from audited artifacts; pipeline exit is not project completion. Future invocation must resolve bundled resources from the installed runtime and must not let an NLP template or a generic reviewer redefine the supplied EM work package.

The useful bounded code/evaluate/revise execution is real; this is not a claim that the autonomous system completed the research or that its reviewer verdict is scientifically authoritative.
