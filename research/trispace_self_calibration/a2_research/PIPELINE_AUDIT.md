# Pipeline intervention log

2026-09-05: Started the installed autonomous workflow from the A2 supplied idea.
The initial novelty stage issued a query expanding SOM as self-organizing maps,
and repeatedly encountered Semantic Scholar 429. This retrieval is scientifically
off-topic. Codex terminated that invocation before any experiment artifacts
were produced. Its folder is retained; no completed results were deleted.

Correction: explicitly expand SOM as subspace-based optimization, keep literature
judgment in the parent ScholarQA evidence workflow, and enter the installed
`run_development_loop` for the already specified A2 task. This preserves its
coding/evaluation/revision/writeup pipeline; it is not a fabricated novelty pass.
The helper records invocation settings and calls existing installed functions.

## Executed development loop and acceptance boundary

The actual installed pipeline entry was `run_development_loop`, launched through
`run_pipeline.py`, with experiment coding delegated to the named
`deepseek-flash` Codex profile. Its bounded artifact root is
`experiments/idea_loops/loop_2026-09-05_17-55-24_a2_development/`.
`pipeline_invocation.json` records the settings and task seed. No default Codex
provider/model was changed. This was not merely a plan to invoke the workflow.

Round-zero produced a useful E1/E2/E3/E5 numerical package. Later revision/writeup
rounds were incomplete and included irrelevant ACL-style review language. They
are not a completed autonomous-paper acceptance. Raw workflow self-evaluations
are not scientific evidence and were not adopted as the manuscript verdict.

The package was independently copied and freshly executed under
`research/delegated/a2_theory_validation/replication/`: 50 bounded tests, new
physical/limited-aperture and negative-control checks, and a physical eight-action
acquisition comparison. Numerical results were audited by Codex. E3 physical
bias Monte Carlo remained unevaluable (0/20), not counted as passing.

The core physical solver, corrected frozen E4, larger-map experiments, passive
bound/enrichment, and corrected measured-data pilot were separate explicit work
packages integrated by the parent. They must not be falsely attributed to an
autonomous loop that generated them all end-to-end. A severe independent review
and parent corrections are preserved in `FINAL_REVIEW.md`.
