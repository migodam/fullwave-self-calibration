# Bounded Q5 reconstruction checks

You are the implementation worker, not scientific orchestrator. Use only the
provided model; no recursive agents/pipeline, no provider calls, no secrets.
Read /Volumes/migodam's-external-brain/Research/Inv_SLAM/Theory/Questions/A5_THEORY.md
and /Volumes/migodam's-external-brain/Research/Inv_SLAM/Theory/Questions/modal_boundary.json.
Write exclusively in your assigned experiment directory. Historical files read-only.

Implement independent SciPy exact electric-dipole sphere coefficients through
the reactance q expression in A5 Section8. Check the two material worlds and
Gaussian binary-risk constants (Section5–7). Reconstruct interval monotonicity
with mpmath.iv and exact decimal endpoints for the two intervals, if available.
No pip/system installs. Existing scientific interpreter:
/Volumes/migodam's-external-brain/Research/Inv_SLAM/research/trispace_self_calibration/a3_research/.venv3d/bin/python
If mpmath is absent, record interval verification NOT DONE; do not substitute
sampled derivatives for interval coverage.

Also test the fixed-loss Born inverse with independently generated full-rank
complex B: reconstruct u,g exactly, check a legitimate parallel u/loss counterexample,
and show deterioration near parallelism. Test correct reference GLS equals
joint gain profiling, including the residual-related derivative term.

Use small single-thread CPU tests and immutable raw JSON; document source hashes,
derivative steps, actual failures and comparison with supplied constants. Do not
claim novelty, hardware reliability, complete Q5, or full material recovery.
Keep detailed outputs in files; final return ≤10 bullets with paths, values,
failures and uncertainty. No figures or paper generation needed for this task.
