# Q5 连续证书与任务窗口：研究审阅

本分支独立于已有 `research/q5-certified-windows-v2`，不覆盖 A5、V1、已撤回选择器或已有 V2 结果。V3 是连续证明/交付版本，不是新注册的算法优越性测试。

## 已成功提交

- `certify_modal.py`：192-bit 整数区间，精确尺寸参数、Taylor 余项、整区间符号包含；由凹性证明端点导数最小值，而非网格推断。
- `sphere_solver.py`：标准 Mie/向量球波双球正演与边界检查，不是新方法。
- `finite_windows.py`：有限任务窗口代数、正确复噪声白化、环带约束 GLS、共享幅度可行区间。有限列表通过不等于连续参数证书。

单独运行已提交的模态证书：

```bash
python research/q5_continuum_v3/certify_modal.py --output /tmp/q5-modal-new.json
```

输出路径必须不存在。源码中的旧示例目录属于冻结时路径；以上命令是当前目录。

## 完整交付与上传限制

完整附件含连续 Maxwell 验证内核、12 场景恢复、29 项检查、七份中文论文/证明/预算/文献修订，以及全部原始数组、失败与源文件哈希。

GitHub 连接器对 `verified_wave.py` 的完整写入和一次正常 blob 重试均返回“无法确定请求的安全状态”拦截。已通过读取确认未写入；未换编码或改路径规避拦截。因此本 PR 不是完整附件的镜像，不能声称“所有代码已通过 PR 提交”。连续证书完整源码应以本次对话附带的研究包为准，不能仅凭此 PR 的三个 Python 文件重放全部证书。

## 结果不能混合

本附件 `run_recovery.py` 源码 SHA-256：`6b1c47133d94a391acc9e94ed03eae1179c90404e3e321952809dda4d8ea8669`；原始观测 SHA-256：`fc0c3ad052fefd8dadb12f0161394f7faa5e2a248730d7dd7f25753c72d4b861`。四主条件的材料通过数为 2、7、2、2 /12，仅要求两个材料误差均 ≤0.1。

已有 V2 `RESULTS_SUMMARY.json` 的 3、5、3、3 /12 来自另一源文件与另一原始数组哈希。相同种子不代表相同数组，不合并，不声称二者差别是算法改进或独立最终测试。

## 科学状态

完整附件中的双有损球有限反例，以固定绝对 proper complex 噪声为前提，经连续体积残差、全部球面解析余项及吸收稳定性界，得到精确均值距离上界约 2.871516376521。这排除所述采集/噪声下整个参数类的统一 99% 材料精度保证，不排除其他采集、更低噪声或受限材料类。

“再增加一个参考足够”仅在事先知道真值属于该两个世界的二元干预类成立。整个连续参数类的最小修复、真实天线预算及原创性审核尚未闭合，不宣称已完成 TAP 投稿。

原始证书和原始恢复已完成，交付检查为 29/29。再次一键重跑触及 180 秒上限，恢复随后显式续跑得到相同计数；连续证书的再次重放也超时，不伪称二次完整复现。所有中断证据保留。
