# Parent-audited E4 implementation

The original worker package and its tuning results remain unchanged in
`../a2_solver`. Python modules are mechanically copied here as a starting point;
parent corrections and new output records are separate. No final E4 seed was
generated before this correction pass. See `AUDIT.md` for changes and freeze.
