# Independent 3D VIE numerical upgrade (bounded development)

Read AGENTS.md. No recursive delegation or pipeline. Use deepseek-flash. Only write research/delegated/a3_maxwell_fft/. Read parent research/trispace_self_calibration/a3_research/maxwell3d.py, validate_maxwell3d_fill.py, calibrate3d.py and results/maxwell3d_fill_development.json. Do not modify those files or A2.

Task: implement a reusable matrix-free FFT dyadic convolution + GMRES solver for the SAME occupied regular-grid dipole VIE. This is known-algorithm engineering, not a new physics model. Match dipole_kernel component ordering, exp(-i wt), polarizability and subcell-volume fill exactly. Embed occupied grid into rectangular grid and zero-pad for linear convolution; verify FFT products vs the existing dense kernel at small grids, including off-origin grids and reciprocity. GMRES solves physical currents (r_free=0), with true residual checked. Count matvecs, iterations, wall, grid/padding, residuals and memory estimate. Existing parent .venv3d has NumPy/SciPy/Treams installed; no installation.

Then run a SHORT independent Treams sphere/cluster convergence audit at k=9 and18, grid spacing .03,.02,.015 (stop if estimated memory>4 GiB or a single solve >120 seconds, preserving failure). Same centers/radii/eps as parent fill study. Treams reference lmax5 vs8 low,8 vs10 high; passive eps. Two transverse polarizations, two directions. Preserve all pointwise complex field errors and resolutions. No inverse sweeps or formal runtime comparison while parent/ROM jobs run. No empirical tuning of physical constants to match Treams.

Goal is a tested numerical route to reduce high-frequency model error, not a fake claim that GMRES tolerance removes discretization bias. If refinement does not reduce field error, diagnose finite-cell polarizability/discretization independently and report. SUMMARY.md + raw JSON + reproduction script + tests mandatory. Final concise paths/findings/uncertainty.
