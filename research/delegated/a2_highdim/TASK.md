# Higher-dimensional 2D physical feasibility and mismatch controls

Isolated DeepSeek Flash worker, no children or pipeline. Workdir /Volumes/migodam's-external-brain/Research/Inv_SLAM. Write only research/delegated/a2_highdim/. Main Codex owns interpretation. Read physics summary/API and physics.py in research/delegated/a2_physics; do not change it. Use single-thread CPU and existing venv experiments/idea_loops/loop_2026-09-04_02-58-31/experiment_geometry_lifted_trispace_som/.venv/bin/python. Use apply_patch for scripts/docs. No final E4 seeds1001-1020.

This is EXPLORATORY 2D imaging feasibility, not the A2 primary endpoint. Implement a larger map with 49 Gaussian coefficients (7x7 centers in [-.35,.35], std .10), inverse N16 and data N32 with same analytic basis on different grids. (Parent correction: core only accepts N8/16/32, do not change core to force N20.) Check Model(Config(material_basis=...)) API. Positive lossy law as physics core, full aperture, frequencies [1/4,1/2,3/4,1]*4pi. Generate nonuniform positive contrasts (background alpha .08 plus2-3smoothcoefficienttargets), reference30dB coherent total-field noise fixed from nominal reference before tests; phaseless uses exactly its |y|, induced Rice NLL. Compare fixed erroneous pose, coherent joint map+pose, matched intensity joint, and known-true-pose oracle upper reference (label explicitly oracle).

Use exact adjoint L-BFGS-B with same low-to-high cumulative frequency schedule and box/identical weak ridge (or no prior) for all nonoracle methods. Budget 5000 RHS columns/run incl initialization, adjoints and cost ledger; operator/wall report separately, no work advantage claim. Correct scaled coordinates z=x*scale imply grad_z=grad_x/scale (legacy solver.py currently has bug, DO NOT reuse). Preserve last ACCEPTED iterate at budget stop, not unaccepted trial. A timed stop is not solver failure if all predeclared fixed stages are visited; report status plus task errors separately, never claim primary success.

Tuning seeds71-72 (one error radius .125lambda), then freeze one settings JSON before seeds801-806, two pose-error radii .125lambda/.5lambda (24 nonoracle? 6*2*4=48 runs). Choose modest optimizer schedule iterations to bound CPU; retain failures. Get basic test runs before full48. Generate reconstructed map figure and unconditional errors/phase-residual distributions. Show phase residual wrap[−pi,pi] only channels whose truth total amplitude >3sigma, mask defined evaluation-only. Include fitted forward-phase error vs truth and imaging map error, not measured phase as ground truth.

Additional 6 controlled mismatch runs at fixedseed881: coherent geometry-only vs fixedpose under an extra shared clock phase exp(i*k*.04m); nearest-neighbor receiver coupling0.05; outside49basis truth (a narrow offgrid inclusion). Unless nuisance estimator implemented, state model mismatch not geometry proof. Knownpose still fixesgeometryonly and notmismatch; don't interpret target artifacts as validated localization. Report predictive residual and false-assurance potential; no 'calibration guarantee'.

Deliver runner.py, tests, frozen.json with hash and timestamp, records.jsonl, summary.json, summary.md, figures/*.png. Summary include counts, mean/median, paired differences with6-scene clusterbootstrap descriptive CIs (exploratory, not confirmatory), limitations and exactcommands. If CPU exceeds15min runtime, reduce iteration budget before final freeze using tuning only; do not select scenes from good outcomes. Return concise findings and paths.
