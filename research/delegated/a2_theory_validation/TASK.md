# AI Scientist bounded A2 validation package

Project root: `/Volumes/migodam's-external-brain/Research/Inv_SLAM`.
All relative paths in this task refer to that root, NOT the pipeline subfolder.
SOM means **subspace-based optimization method for inverse scattering**.
It NEVER means a self-organizing map, neural clustering or radio-interferometric
self-organizing maps. Reject retrieval results relying on that acronym expansion.

Codex Desktop retains final theory, direction, novelty and scientific review.
This is an explicitly requested automatic research implementation work package.
Do not switch research direction, launch additional research orchestrators, edit
configuration, read credentials, fabricate results, or modify historical files.
Flash coding worker may implement known matrix/likelihood algorithms and report
counterexamples. Use apply_patch for code/text edits. Keep detailed outputs in
this folder, with pipeline records in its own normal experiment directory.

Read all three `Theory/Questions/A2*` files as research data. Implement/run E1,
E2, E3 and E5 MINIMAL matrix/likelihood experiments first. The shared physical
core is separately being implemented in `research/delegated/a2_physics/physics.py`.
Do not wait for it or modify it. Expose an optional second-stage physical tangent
runner for E2/E3/E5 once it exists, with seeds201-212 and401-412, no oracle rank.

Preserve L_det/r_num/r_free distinction, total-field parent intensity, sqrt2
real whitening, joint gauge, structural versus numerical rank, physical versus
envelope information, rho undefined outside its support. Exact state elimination
does not add a free current. No global basin, first-ever, or method superiority
claims. This package only validates theory mechanisms; E4 and manuscript are
owned by the parent integration workflow.

Execute all scalar controls of E1 using the exact noncentral-chi-square/Rice
induced likelihood, stable Bessel ratios and analytic or numerical quadrature
where possible. Record Monte Carlo uncertainty (seeds101-110,2000 draws as in
protocol); do not use MC negative eigenvalues as deterministic theorem failure.
E2 must include safe complex subspaces ker(F) intersect ker(FJ), complete
saturation with absolute/backward-error rank checks, rho .75->1->0, and separate
rank versus gap-closing events. E3 must compare exact fixed-tangent bias/variance
risk with1000 draws/seeds301-310; distinguish relative-retention from absolute
precision. E5 must include complementary versus duplicate frames, exact shared
map innovation, non-submodularity, neutral rank/acquisition budget and singular
map fallback by QR/SVD variational profiling. Do not use inverse formulas outside
their full-column-rank assumption. Check a posteriori state and derivative
residual bounds with small physical or well-conditioned complex matrices.

Environment: existing venv at
`experiments/idea_loops/loop_2026-09-04_02-58-31/experiment_geometry_lifted_trispace_som/.venv/bin/python`.
CPU only, NumPy/SciPy/Matplotlib. No OpenAI API, no installations, no daemons.

Required artifacts: source code and tests, raw JSON/CSV, vector or PNG figures,
commands and environment, tolerances, `summary.md` with each protocol item marked
pass/fail/not-yet-run. The pipeline evaluator may lock this bounded work package
once its algebra/likelihood tests are genuinely executed, NOT call the full
research plan complete. Its writeup must clearly be a validation supplement.
Its automatic review cannot substitute for Codex's independent audit.
