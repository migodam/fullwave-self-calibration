# Second development pass: chart reuse and fair strong baselines

Read AGENTS.md. Isolated Flash worker, no delegation. Write ONLY research/delegated/a3_rom_reuse/. Existing a3_rom and all A2 source/results are read-only. Read a3_rom/SUMMARY.md, common.py, wave.py, basis.py, solver.py. Parent accepts algebra foundation but rejects universal SOM conclusions.

Main issue to correct: existing solver rebuilds charts EVERY accepted iterate. Implement a reuse controller in a new module importing the existing modules read-only. Freeze chart during each derivative; reuse across accepted points while the physical state relative residual <=1e-3 and relative predicted-output discrepancy against the exact-acceptance forward <=1e-3; if guard fails rebuild/enrich, then direct fallback if still invalid. Exact forward is already budgeted acceptance; no exact derivative oracle online. Record cache age/hits, rebuild causes, and work. All ROM variants share guards, enrichment rules and budgets. Do not count a multi-frequency accepted step as proof that its high-frequency derivative is accurate: record separate offline high-frequency state/output/map/pose derivative errors at accepted steps on development runs.

Critical fixes for fair evidence: report rank-deficiency including target zeros; direct adjoint must respect budget BEFORE the expensive call rather than overshoot it; include setup cost, accepted iteration states, all SVDs/QRs (existing _task_directions has uncharged map-RHS SVD), and precomputation/cold vs warm explicitly. Use same final data and optimizer stopping accuracy. No claim exact-intersection Twofold should be a baseline (it is not the Chen sequential construction).

First n16/q9 seeds4101-4104, no continuation, compare direct_gn/direct_adjoint/generic_task/twofold_task. Then bounded n32/q49 development seeds4201-4202 using configurable Gaussian material basis via Config, if feasible <120s/run. Compare same r budgets and also minimum rank meeting development derivative criteria; do not cherry-pick only cost-favorable configs. No formal timing while other compute jobs run; label development timings. No final holdout sweep yet.

Deliver tested controller, derivative/guard tests, raw JSON, SUMMARY.md with all failures, and a proposed frozen final protocol including success thresholds and scene-level CI. Do not make novelty or final superiority verdict. Parent will decide.
