# 命题—假设—证明—证据台账

这是本轮父级汇总；不能把程序输入检查、回归测试数或数值网格点数称作定理验证数。A5/V1 原始文件不改动。

|编号与结论|精确范围/必要假设|证明或实现|实际证据|裁决|
|---|---|---|---|---|
|F1：坐标观测纤维直径 ≤2δ 为有限误差估计的充要条件|有界允许观测集；坐标绝对损失；输出在矩形中，不必对应联合物理状态|修订章节 §2，三角不等式与坐标中点|解析证明|成立；通用集合估计，不主张新颖|
|G1：增益消去后的有限射影弦长公式|精确规则电偶极张量；同一公共复系数|§3.1 行列式证明；方向端点凸性|`certify_geometry_window.py`，代数数值交叉检查|成立于声明模型|
|G2：名义 kR∈[0.6,1.6] 的径向窗口|径向先验半宽0.1；九复分量；信号范数/σ≥600；噪声范数≤5σ；三激励相对幅相已知|连续名义箱、全部有限分离对盒、方向多项式正性|`geometry_window_certificate.json`|有限条件证书；非完整位姿，非 C，非最大窗口|
|G 材料窗口仍可为空|ka=0.15；ε=4.70/4.91；公共复增益环带[.75,1.25]|精确选 g′=a(4.70)/a(4.91)|`geometry_readout_budget.json` 的合法增益包含|有限反例；与 G2 并存|
|M 单调性与有限导数界|两个分别读出的无损已知球；名义及±100ppm尺寸域|160位整数区间、明确三角余项、11000个连续盒|两份 `modal_*certificate`/`modal_size_100ppm` JSON|补齐本轮指定证明缺口；没有端点最小值断言|
|32点径向模态读出与无限尾界|完整球面理想点读出；已知中心；kR=1；指定平面波；mx≤1、x≤1/4、实 m|§6 正交多项式求积及全尾项级数证明|`certify_readout.py`，VSW独立数值核对|理想有限读出成立；硬件容差未验证|
|16次重复下两材料误差≤0.1|表列噪声、节点/照明/参考漂移/尺寸/其余误差上限同时成立|§6.2 比值不等式；完整乘性交叉项|误差上界0.056652、0.092714；联合事件概率>0.997|条件充分证书；单次第二材料失败；16倍资源不是算法优势|
|Mη：1.25%相对路径误差可产生精确竞争世界|仅第二模态路径有额外未知复传递；共同参考精确|§7 显式世界构造与区间 η*=0.012293532…|`readout_certificate.json`|精确有限反例；非 C；非一般 Maxwell 几何扰动|
|参考干预类中的相对最小补救|仅增加参考，不改变 EM；已有共同幅度锚点|一个第二路径幅度参考充分；再多共同参考仍有同一反例|解析单调性证明|仅此干预类的必要/充分性|
|共享 nuisance 融合恒等式|局部白化模型；共享 nuisance；展示式暂假设各 H 正定|§4 完成平方|回归核对 W1|标准代数，不作为新方法|
|G→M 误差预算不能自动拼接|电偶极径向传递；几何误差指标不同|精确 Hankel 比值、完整导数区间|±0.02 对应约4.92%–5.29%复传递变化|联合实现缺口明确，未假装闭环|
|C 新冻结独立恢复|12个新场景；DDA生成、Mie多球反演；相同初值与限额|协议及实现锁定后执行|2/12、7/12、2/12、2/12；全部原始观测与初值保留|开发诊断，不是最终测试，不是新算法优势|
|C 连续有限分离与有效误差下限|必须有连续 Maxwell 模型界、完整竞争对覆盖|本轮只有18个优化候选及阶数/网格差|`finite_worlds_diagnostic.json`|未解决；优化距离不是分离下界|
|物理可达性与原创性|真实探头/背景/相干链路；最近邻全文|文献对照与验收预算|部分全文已核读；没有硬件测量|未全部完成；不得宣称 TAP-ready|

## 本轮审计修正

首次读出预算漏了节点校准×照明校准的乘性交叉项。原程序与原输出完整保存在 `audit/readout_initial_missing_cross_terms.*`，现行 `certify_readout.py` 和 `results/readout_certificate.json` 已补齐，本文只使用修正值。

第一次单元测试的失败是将化简后的有理端点（3/5）与未化简格式（6/10）直接比较；改为比较有理数值，未改任何科学结果。原失败日志保留。18项当前回归检查通过，但它们不是18条正式定理。

恢复运行曾在200秒工具限额终止，原始数据已全部生成、11场景已完成。续跑只读取既有数据、跳过既有场景，补做第12场景和注册的收敛诊断；没有重新抽样或调参。原始/续跑日志保留，总端到端时间不从续跑时间冒充。
## 交付前调用计数修正

原始 `forward_evaluations` 字段统计的是残差回调，不是线性系统求解次数。附加 EM 分支每次回调两次场评价。`audit/CALL_COUNT_AUDIT.json` 分开列出两者，保留原始结果不重写。完整新目录复现通过 18 项测试；原始观测哈希和确定性证书一致，不增加统计样本量。
