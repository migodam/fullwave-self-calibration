# V2 结果后的归因控制：单独诊断，不作方法竞赛

本控制在已看到 V2 的 3/12、7/12、3/12、3/12 材料结果后提出，因此明确属于事后机制诊断，不修改原协议、原数据、主结果或成功阈值，不并入最终测试。

沿用已保存的十二个场景、原始噪声、原参考和三个初值/优化预算，执行以下固定控制：

1. 把 DDA 无噪真场替换成 L=7 完整双球 Mie 真场，保留原噪声与参考；分别无参考和 noisy-reference GLS 反演，检验移除生成/反演模型不一致后的变化。
2. 原 DDA 带噪数据：仅真实增益模长已知、真实复增益已知、真实复增益及几何均已知，三个 oracle 控制依次区分幅度锚定、剩余相位/几何补偿和材料拟合偏差。
3. DDA 无噪数据、真实增益及几何已知，直接检验结构模型偏差能否单独造成材料失败。
4. 原 DDA 带噪数据、参考改为原参考乘 1.02 exp(0.03i)，作为固定错误参考压力测试；这不是未知错误参考的自动检测算法。

所有控制用原始绝对 sigma 进行同尺度报告，双材料误差均 <=0.1 才记成功。oracle 含真值信息，绝不能声称是可部署方法或公平基线。保存每个初值、失败、残差、材料/位移/增益误差和代码哈希。结果不用于重调已撤回的 selector。
