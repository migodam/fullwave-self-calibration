# V2 执行细节冻结

补充 PROTOCOL.md 的未指定实现细节，不改变方法、预算、样本量或成功标准。本文件提交时尚未生成 V2 恢复结果。

- 先用随机流 2026091107 生成全部 12 个场景的真实材料、位移和增益，再用同一流产生观测噪声；真实参数在生成观测前固定。
- 固定附加 EM 标量为按 (receiver, Cartesian component, illumination) 展平后的第 0 个分量。随机附加标量使用独立流 2026091108，均匀选自 192 个候选分量。参考与附加 EM 各消耗一个新增复标量，不宣称硬件成本相同。
- Treams 安装因执行环境没有网络解析而失败；使用协议允许的独立 SciPy 向量球谐实现。主模型 L=3、角投影 Gauss-Legendre 14 × 28 点。前四场景的数值诊断包括 L=5、L=7、L=3 的 22 × 44 点角积分以及 DDA spacing=0.009、0.008 米；差值不是连续误差上界。
- 运行前已通过 5 项数值正确性测试：向量球谐正交、复介质球切向 E/H 边界条件、平面波重建、带环带增益 profiling 与 GLS 恒等、阶数/角积分一致性。测试只是数值检查，不是 Maxwell 连续误差证书。
- 完整输出保留真实场、带噪观测、全部候选附加标量、参考、所有初值的优化状态、材料误差、位移误差、增益误差、固定世界坐标结构场误差、运行时间和源码哈希。
- 本轮可能另行搜索有限竞争世界。搜索值仅为距离下确界的上界；除非有独立连续前向误差上界，否则不得标记为通过的 Maxwell 不可能性证书。数学见证搜索不计入上述统计诊断流，也不被用于调参重跑该流。
