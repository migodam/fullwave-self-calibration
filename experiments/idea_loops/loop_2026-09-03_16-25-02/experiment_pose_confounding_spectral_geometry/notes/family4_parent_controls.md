# Family 4 parent controls

The raw trajectory-ordering hypothesis remains false.  Frequency diversity was rerun after matching each frequency block's nominal signal norm; the three selected retentions become [0.117035, 0.215653, 0.446042].  Trajectory rankings are reported under identity noise, per-pose SNR matching, equal continuous design length, and a same-radius curved-path control. Differences between these rankings demonstrate objective/metric and standoff sensitivity, not a universal ordering.
